# 工作总结 / Work Summary

## 当前分支 / Current Branch

- 分支：`HW3/4-NAG`
- 硬件：LILYGO T-2CAN，ESP32-S3 + TWAI + MCP2515
- 构建环境：`lilygo_t2can_arduino_webui`
- Flash：16MB，DIO
- PlatformIO 平台：pioarduino `54.03.21`
- 工具链：GCC `14.2.0`

## 当前功能 / Current Features

- V12/V13（HW3）与 V14（HW4）运行时选择。
- FSD 激活帧处理与可配置周期补发。
- HW3/HW4 驾驶模式和速度偏移控制。
- HW4 跟车距离到五档驾驶模式映射。
- HW4 ISA 提示音抑制与附加 Autopilot 功能。
- 免打扰滚轮兜底；V12/V13 同时处理座舱摄像头配置，V14 不处理。
- BODY CAN 灯光、维修模式、电池预热等现有功能。
- 本地 WiFi SoftAP、WebUI 自动保存和 CAN/系统诊断。

## 总线 / CAN Mapping

- `bus=1`：ESP32-S3 TWAI，物理 CANB，主要接 CH CAN。
- `bus=2`：MCP2515，物理 CANA，主要接 BODY CAN。
- TWAI：TX GPIO7，RX GPIO6。
- MCP2515：SCK12，MOSI11，MISO13，CS10，RST9，INT8，16MHz。

## 2026-07-12 清理与独立导出 / Cleanup and Standalone Export

- 删除与当前固件构建无关的旧分析文档、旧 RP2040 源码、历史校验文件和编辑器规则。
- `platformio.ini` 只保留 LILYGO T-2CAN WebUI 环境。
- 固定 16MB Flash、DIO、240MHz、OPI PSRAM 和 pioarduino `54.03.21`。
- 删除含本机绝对路径的 `platformio.isolated.ini`；隔离构建改用 `PLATFORMIO_CORE_DIR`。
- 从头重建 `README.md`，记录硬件、总线、功能、构建和下载方法。
- 保留 `AGENTS.md` 与本文件作为仓库维护记录；独立发布目录不包含它们。
- 固件源码、CAN 位定义、收发周期和运行逻辑未修改。
- JavaScript 语法检查通过。
- 清理后的源项目编译成功：
  - RAM：47,300 / 327,680 bytes（14.4%）
  - Flash：1,062,594 / 3,145,728 bytes（33.8%）
- 独立项目已导出到：
  - `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\HW3-4-NAG-clean-20260712`
- 独立项目仅包含 `.gitignore`、`README.md`、`platformio.ini`、`huge_app.csv` 和两个固件源码文件。
- 独立目录再次编译成功：
  - RAM：47,300 / 327,680 bytes（14.4%）
  - Flash：1,063,810 / 3,145,728 bytes（33.8%）
- 未提交、未推送、未擦除、未下载固件。

## 2026-07-13 分支功能审计 / Branch Feature Audit

- 重新读取全部本地与远端 Git 引用，并按源码 Blob、提交谱系和独有提交核对功能。
- 本地共有 27 个分支名、23 个不同顶点；若干 `claude/*` 分支是功能相同的工作别名。
- 分支主要分为：
  - Waveshare ESP32-S3 单 TWAI：`main`、`ESP32-FSD`、`ESP32-HW3`、`ESP32-HW4`、`codex/hw3-speed-optimization`。
  - RP2040 + MCP2515：`RP2040CAN-FSD`、`RP2040CAN-HW3`。
  - LILYGO T-2CAN 基础与实验：`T-2CAN-WEB`、`T-2CAN-WEB-optimization`、`BD_ON_CAN1`、`codex/can-a-full-recorder`。
  - 扭矩/联网分支：`nag-killer`、`NAG-KILLER-AP`、`codex/torque-nag`、`NAG-2CAN`、`NAG-2CAN-BODY`。
  - 后期 HW3/HW4 WebUI 分支：`codex/disable-telemetry`、`codex/HW4-HW3`、`NAG`、`NAG-HW4`、`HW3/4-NAG`。
- `codex/3-can` 与 `nag-killer` 的固件源码 Blob 完全相同，只增加 P4 三 CAN 适配待办和导出记录，尚未实现三 CAN 固件。
- `codex/disable-telemetry` 的当前顶点已经删除 Telemetry 改写功能；分支名保留，但当前主要功能是 FSD 激活帧补发与精简后的 WebUI 控制。
- `NAG`、`NAG-HW4`、`HW3/4-NAG` 中仍有旧扭矩 NAG 源码残留，但运行配置会强制关闭，当前不属于可用功能。
- 本次仅分析并更新工作总结；未修改固件源码、未编译、未提交、未推送、未下载。

## 2026-07-14 README English Supplement / README 英文补充

- English:
  - Added a full English section to `README.md` for the current `HW3/4-NAG` branch.
  - Covered hardware, CAN mapping, WebUI, HW3/HW4 FSD activation, resend behavior, DND, HW4 profile/offset, BODY CAN features, diagnostics, build, and upload notes.
  - Kept the existing Chinese README content.
  - Documentation-only change; firmware source was not modified.
- 中文:
  - 已在 `README.md` 中补充当前 `HW3/4-NAG` 分支的完整英文说明。
  - 覆盖硬件、CAN 映射、WebUI、HW3/HW4 FSD 激活、补发、免打扰、HW4 驾驶模式/速度偏移、BODY CAN 功能、诊断、构建和下载说明。
  - 保留原有中文说明。
  - 本次仅修改文档，未改固件源码。
