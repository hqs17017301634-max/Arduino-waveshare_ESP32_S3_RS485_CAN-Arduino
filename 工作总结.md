# 工作总结 / WORK SUMMARY

> 规则：每次开工**先读本文件**再动手；每次完成改动**回来更新本文件**。
> 用途：跨会话保留上下文，减少压缩损失。

最后更新：2026-06-04

---

## 0. 两个项目 / 分支（别搞混）

| 项目 | 路径 | 框架 | 当前分支 | 内容 |
|---|---|---|---|---|
| **RP2040**（当前主战场） | `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040` | Arduino / PlatformIO | `T-2CAN-WEB` | 双 CAN + 轻量 WebUI + 灯光爆闪 + 录制 |
| **T-2CAN**（ESP-IDF dashboard） | `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\T-2CAN` | ESP-IDF / PlatformIO | `main` | 完整 dashboard，已做高光爆闪/0x249 |

- `T-2CAN-WEB` 分支**只在 RP2040 项目里**；T-2CAN 项目没有这个分支。
- 提交由**用户自己管理**，AI 改完默认**不自动提交**，除非用户说“提交”。
- 不要提交：`.pio/`、`Downloads/`、`opendbc-master/`、`源代码与抓包数据/`。

## 1. 关键编译命令（RP2040）

```powershell
cd C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040
pio run -e lilygo_t2can_arduino_webui      # 带 WebUI（灯光/预热功能都在这）
pio run -e lilygo_t2can_arduino            # 不带 WebUI
pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23
```
WebUI 热点：SSID `T2CAN-FSD` / 密码 `12345678` / **`http://100.100.1.1`**（softAPConfig 设定，2026-06-03 起）

## 2. 主要文件（RP2040）

- `ESP32S3CAN-FSD/ESP32S3CAN-FSD.ino` — 主固件（CAN A FSD + CAN B 灯光 + WebUI + 录制）
- `ESP32S3CAN-FSD/web_ui_page.h` — WebUI 页面（**手写**，不经 minify）
- `README.md` — 中英文文档

## 3. ⚠️ 已知坑

- **`.ino` 是 UTF-8，但中文注释是 GBK 乱码（mojibake）**：被非 UTF-8 编辑器存过。
  能正常编译。AI 改这个文件时：**只匹配 ASCII 行**，新注释**写英文**，别加中文，避免加重乱码。
  （`web_ui_page.h`、`README.md` 是干净 UTF-8，可正常写中文。）
- 待办：可单独把 `.ino` 转回干净 UTF-8（用户未要求时先不动）。

## 4. CAN B 灯光/拨杆 关键报文与字段（实车抓包结论）

- **0x249 SCCMLeftStalk**：`byte1` 低 4 位=counter(0-15 滚动)，bits4-5=status；
  status=1=PULL(超车灯/Flash-to-pass)，0=idle。`byte0 = BASE[counter] XOR OFFSET[status]`（已破解）。
  → 用于**高光爆闪**（双拉触发，8 次）。
- **0x3C2 VCLEFT_switchStatus（多路复用！）**：`byte0 bits0..1`=mux 索引。
  - **mux0**(`b0&3==0`)：承载 `byte0 bit3`=hazardButtonPressed(按下=0x08) + 计数器/CRC；此 mux 的 `data[3]=0x55` 是填充，**不是滚轮**。抓包按双闪 = `08 55 55 55 00 00 59 45`。
  - **mux1**(`b0&3==1`)：承载 `data[3]`=rightScrollTicks(6-bit 有符号，空闲 0，+前/-后)。空闲样例 `41 85 0 0 0 0 0 128`。
  - ⚠️ **坑（cap64 实测）**：不分 mux 直接读 data[3] 会把 mux0 的 0x55 误读成 +21 假滚轮，踩刹车换 R 时触发“D 意图→取消”，导致倒车双闪只闪 1~2 下就停。**已修**：滚轮只在 mux1 读；双闪注入只在 mux0（缓存最近 mux0 帧）。
  → 用于**双闪注入(mux0)** + **右滚轮+刹车 档位识别(mux1)**。
- **0x3F5 VCFRONT_lighting**：只读灯态反馈，**不注入**。
  （byte3 bit5 远光实点亮 / bit4 自动远光 / byte4 bit5 手动远光 / byte1 bit7 大灯手动模式）
- **0x273 body lighting**：后雾灯，复用最新实时帧只切 `data[2] bit7`（0x80）。
- **0x082 UI_tripPlanning**：电池预热。ON=`AF 50 AC 3C FF 03 9A 0F`，OFF=`01 50 AC 3C FF 03 9A 0F`，周期 1000ms。
- **0x109 SBW_RQ_SCCM**：换挡请求帧，**CRC/档位值未知，禁止伪造注入**（风险高）。需要再抓右拨杆 D/R 才能破解。

## 5. 各功能现状（RP2040 / T-2CAN-WEB）

| 功能 | 配置开关 | 状态 |
|---|---|---|
| 高光爆闪 | `highBeamStrobeEnabled` | ✅ 双拉 0x249 PULL 触发 8 次 75/75ms |
| 后雾灯刹车爆闪 | `rearFogBrakeStrobeEnabled` | ✅ 0x273，缓减速 3 次/急减速 6 次 |
| 倒挡双闪+后雾灯 | `reverseStrobeEnabled` | ✅ 触发=0x118 R 档 或 刹车+0x3C2 右滚轮向后；双闪改 **toggle 状态机**：按一下开(200ms)→保持 2500ms 连续闪→再按一下关；雾灯走 0x273（**双闪仍待实车验证**） |
| 电池预热 | `batteryPreheatEnabled` | ✅ 0x082 实车 ON/OFF payload，1000ms |
| 滚轮换挡仿真 | `scrollGearSimEnabled`(默认开) | ✅ 仅显示 D/R 意图，不发帧 |
| 滚轮换挡注入(实验) | `scrollGearInjectEnabled`(默认关) | ⚠️ 注入 0x229，**实车被拒**(counter 冲突, 见 §15-16)；安全门控=刹车+≤2km/h+冷却 |
| **CAN1 只收不发** | `can1ReceiveOnly`(默认关) | ✅ 打开后屏蔽 CAN A 全部 TX(`twai_send` 入口拦截)，只保留 RX/录制；接 PARTY 等陌生总线嗅探时用 |
| CSV 录制 | — | ✅ `/rec_*`，默认 ID 含 082/249/273/3F5… |

## 6. 最近一次会话做了什么（2026-06-03）

**已提交并推送到 GitHub**：`57a71d6`（倒车爆闪/后雾灯/电池预热初版）+ `7ebe69e`（本次修正）。
origin = `github.com/hqs17017301634-max/waveshare_ESP32_S3_RS485_CAN-speed-offset-slew`，分支 `T-2CAN-WEB`。

本次（`7ebe69e`）依据实车抓包(cap64)修正：
1. **电池预热**：`0x082 data[0]=0x05`/500ms → 完整 ON `AF 50 AC 3C FF 03 9A 0F` / OFF `01 50 AC 3C FF 03 9A 0F` + 1000ms + 关闭补发 OFF 帧。
2. **双闪**：删 `stalkTurnFrame`(0x249 turn-field)，新 `vcleftHazardFrame()` 走 `0x3C2 byte0 bit3`，且**只在 mux0** 注入(缓存最近 mux0 帧)。
3. **右滚轮+刹车 档位识别**：`0x3C2` **只在 mux1** 读 `data[3]` 6-bit 有符号 + `g_brakePedalActive`(来自 0x145)；
   刹车+后滚(<0)→R 触发，刹车+前滚(>0)→D 取消；**不发 0x109**。`0x3C2` 已加入 CAN B RX 过滤(RXF5)。
4. **修复假滚轮 bug**：旧代码不分 mux 把 mux0 的 0x55 误读成 +21，换 R 时自我取消(只闪1~2下)。已按 mux 区分解决。
5. **倒车双闪节奏**：实车发现 0x3C2 是**瞬时 toggle 按钮**，脉冲 4 次=开/关/开/关(只看到 2 段亮)。
   改为**状态机**：按一下开(`REVERSE_HAZARD_CLICK_MS`=200ms) → 保持 `REVERSE_HAZARD_ON_MS`=2500ms(车连续闪) → 再按一下关。后续若仍不对，可能要按实车 hazard 反馈(0x3F5)判断当前状态。
6. **WebUI SoftAP IP**：`softAPConfig` 设为 **100.100.1.1**（页面 `http://100.100.1.1`）。
- 同步更新 `web_ui_page.h` 提示 + `README.md`（中英文，含 IP 与 0.5s 节奏）。
- 编译通过：`lilygo_t2can_arduino_webui` SUCCESS，Flash 23.8%；已烧 COM23。

## 7. 待办 / 下一步

- [x] `.gitignore` 加入 `Downloads/`、`opendbc-master/`、`源代码与抓包数据/`（解决 IDE “+7M 变更”假象）。
- [x] 本次改动已提交 `7ebe69e` 并推送 origin。
- [x] **CAN A 全量抓包实验分支已做**：分支 `codex/can-a-full-recorder`，新增环境 `lilygo_t2can_cana_full_recorder`。
  目的：抓 `0x109 SBW_RQ_SCCM`。此环境绕过 CAN A 软件 ID 过滤、每轮 loop drain 更多 TWAI RX、强制关闭 FSD/速度偏移/CAN B 灯光/倒车爆闪/电池预热等 TX 功能。WebUI 抓包 ID 留空=全量抓；填 `109`=只抓疑似换挡帧。
  烧录：`pio run -e lilygo_t2can_cana_full_recorder -t upload --upload-port COM23`。恢复正常：烧回 `lilygo_t2can_arduino_webui` 或切回 `T-2CAN-WEB`。
- [ ] **用滚轮真正挂挡（用户选定目标）**：拿到 `0x109` 抓包后，再破解档位值+CRC(可能是标准 `teslaCanChecksum`)。⚠️ 注入换挡命令安全攸关，需严格门控。
- [ ] **实车验证**：0x3C2 双闪按钮是瞬时切换语义；4 次按/松可能只显示 2 段亮，必要时改“按一下开→保持→再按一下关”。
- [ ] （可选）把 `.ino` 编码转回干净 UTF-8。
- [ ] T-2CAN(ESP-IDF) 项目的高光爆闪提交 2978f47 在 `main`，是否需要单独分支由用户定。

## 8. 本地操作记录（2026-06-04）

- 当前分支：`codex/can-a-full-recorder`。
- 已烧录 COM23：`pio run -e lilygo_t2can_cana_full_recorder -t upload --upload-port COM23`。
- 烧录结果：SUCCESS，目标芯片 ESP32-S3，Flash 写入校验通过，Hard reset 完成。
- 当前板上固件：CAN A 全量抓包实验固件，用于抓 `0x109 SBW_RQ_SCCM`。WebUI 抓包 ID 留空=全量抓；填 `109`=只抓疑似换挡请求。
- 抓包分析：
  - `can_recording (67).csv`：D/R/D 档位抓包，`0x109` 计数为 0；`0x118` 明确显示 gear 4(D) -> 2(R) -> 4(D)，时间约 77351/78700/82399 ms。
  - `can_recording (68).csv`：右滚轮前/后滚抓包，`0x109` 计数为 0；`0x3C2 mux1 data[3]` 解码为 6-bit signed，前滚为正数 `+1/+2/+3/+4`，后滚为负数 `-1/-3/-5`。
  - `0x3C2 mux0 byte7 bit4` 可作为刹车状态来源：67 中 78075-80304 ms、81714-84034 ms 为 brake=true；68 中未踩刹车。
  - 结论：当前已接 CAN A 上没有看到 `0x109 SBW_RQ_SCCM`；实际换挡请求帧很可能在其它总线，或车型/固件中不是 `0x109`。
- DBC 对照：
  - `tesla_model3_vehicle.dbc` 定义了 `0x3C2 VCLEFT_switchStatus`、`0x118 DI_systemStatus`、`0x553 SCCM_rightStalk`、`0x568 STW_ACTN_RQ`，都属于 `VEH` 网络；当前抓包能看到这些 ID，说明当前接入 A 路对应 Model 3/Y 的 `VEH`/vehicle CAN。
  - `0x109 SBW_RQ_SCCM` 不在 `tesla_model3_vehicle.dbc`，而在老 `tesla_can.dbc`；发送节点为 `STW`，信号接收方标为 `NEO`，更像旧架构 steering wheel/SCCM 相关总线，不是当前 Model 3/Y `VEH` 总线。
  - 因此按 DBC 不能把 `0x109` 归到当前 A 路；若要继续找真正 D/R 请求，需要抓 SCCM/方向盘局部总线、DI/SBW 相关总线，或更多车身/网关侧总线。

## 9. 实车接线 / CAN 路径备注

- X197 Pin 13 / 14：CH CAN，当前 FSD 激活路径在这一路。
- X197/X179 Pin 9 / 10：BD CAN，当前雾灯、远光/高光爆闪等灯光路径在这一路。
- Pin 2 / 3：PARTY CAN。
- 当前 `lilygo_t2can_cana_full_recorder` 已修改为 CAN A/B 双路抓包：CAN A 可接 Pin 13/14 抓 CH CAN，CAN B 可接 Pin 9/10 抓 BD CAN；CSV 中 `bus=1` 是 CAN A，`bus=2` 是 CAN B。
- 若要同时验证灯光/车身信号，应使用普通 `lilygo_t2can_arduino_webui` 或新增 A+B 全量抓包实验环境，让 CAN B 接 Pin 9/10 抓 BD CAN。
- 若要找 `0x109`，优先不要只盯 CH CAN；需要结合 DBC 和实车接线继续尝试 BD CAN、PARTY CAN 或 SCCM/方向盘/DI-SBW 相关局部总线。

## 10. 当前修改记录（2026-06-04）

- 分支：`codex/can-a-full-recorder`。
- 已修改实验固件为 A/B 双路全量抓包：
  - `ENABLE_CANA_FULL_RECORDER` 下不再强制 `canbEnabled=false`，改为 `canbEnabled=true`。
  - 仍然强制关闭 FSD、速度偏移、CAN B 灯光 TX、倒车爆闪、电池预热等发帧功能。
  - CAN A 继续绕过软件 ID 过滤并提高 TWAI RX drain。
  - CAN B 保持开启，硬件过滤关闭，记录到 CSV `bus=2`。
- 编译验证：`pio run -e lilygo_t2can_cana_full_recorder` SUCCESS。
- 已烧录 COM23：`pio run -e lilygo_t2can_cana_full_recorder -t upload --upload-port COM23`，结果 SUCCESS，Flash 写入校验通过，Hard reset 完成。

## 11. Capture analysis 2026-06-04: can_recording 69/70

- Files:
  - `can_recording (69).csv`: brake held before capture, then D/R shifting.
  - `can_recording (70).csv`: right scroll wheel forward/backward.
- Firmware/capture mode: `codex/can-a-full-recorder`, A/B dual full capture.
- CSV bus mapping:
  - `bus=1`: CAN A, connected to CH CAN in the current test plan.
  - `bus=2`: CAN B, connected to BD CAN in the current test plan.
- Counts:
  - 69: 38987 rows, 12622 ms, bus1=29553, bus2=9434.
  - 70: 17915 rows, 5810 ms, bus1=13598, bus2=4317.
- `0x109 SBW_RQ_SCCM`: not found in either 69 or 70 on either bus.
- 69 gear state feedback:
  - `0x118 DI_systemStatus` appears on both bus1 and bus2.
  - Gear sequence while brake pressed: P(1) -> D(4) -> R(2) -> D(4) -> R(2).
- 69 shift request/source:
  - `0x229 SCCM_rightStalk` appears only on bus2.
  - Non-idle values correlate with the actual D/R shifts:
    - status 1/2 around 560184-560387 ms, then gear becomes R.
    - status 3/4 around 563203-563483 ms, then gear becomes D.
    - status 1/2 around 565829-565987 ms, then gear becomes R.
  - DBC note: decimal 553 = hex 0x229, not 0x553.
  - Interpretation: for this car/current wiring, actual right stalk gear request is on BD CAN `0x229 SCCM_rightStalk`, not `0x109`.
- 70 right scroll wheel:
  - `0x3C2 VCLEFT_switchStatus` mux1 right scroll ticks appear on bus1.
  - Forward scroll decoded as positive ticks: +1/+2/+3.
  - Backward scroll decoded as negative ticks: -1/-2/-3/-4 using 6-bit signed `data[3] & 0x3F`.
  - bus2 `0x3C2` frames in this capture are only mux0 hazard/switch frames (`00 55 55 55 00 00 5A 45`), no mux1 scroll ticks.
- Practical conclusion:
  - Right scroll intent should continue to use `bus1 / 0x3C2 mux1`.
  - Actual stalk D/R request, if later injected or emulated, needs `bus2 / 0x229 SCCM_rightStalk` CRC/counter cracking and strict safety gates.
  - Do not spend more time on `0x109` for the current Model Y CH+BD capture path unless another physical bus is added.

## 12. Scroll wheel gear shift experiment 2026-06-04

- Branch: `codex/can-a-full-recorder`.
- Implemented first-stage WebUI/firmware support:
  - Decode and expose `0x229 SCCM_rightStalk` status/counter on CAN B.
  - Decode and expose `0x3C2 mux1` right scroll ticks.
  - Expose `0x118` current gear, brake state, and vehicle speed.
  - Add WebUI switch `scrollGearSimEnabled`: dry-run display only, default ON.
  - Add WebUI switch `scrollGearInjectEnabled`: experimental 0x229 injection, default OFF.
- Injection design:
  - R request: `0x229` status sequence `1 -> 2 -> 2 -> 2 -> idle`.
  - D request: `0x229` status sequence `3 -> 4 -> 4 -> 4 -> idle`.
  - `byte1 = (status << 4) | counter`.
  - `byte0 = base[counter] ^ offset[status]`, derived from capture 69.
  - Safety gates: brake held, speed valid and <= 2 kph, current gear != target, cooldown, WebUI injection switch ON.
  - Scroll trigger is edge-latched: one nonzero scroll burst triggers once, then requires ticks to return to 0.
- Build verification:
  - `pio run -e lilygo_t2can_cana_full_recorder`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Upload:
  - Flashed normal WebUI firmware to COM23 with `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`.
  - Result: SUCCESS, ESP32-S3 detected, flash verified, hard reset complete.
- Important safety note:
  - The normal WebUI firmware contains the experimental injection path but the injection switch defaults OFF.
  - The full-recorder build forcibly disables injection via build guards.

## 13. Scroll gear test issue 2026-06-04

- User reported scroll gear test ineffective.
- Root cause found in code/capture mapping:
  - Capture 70 shows right scroll ticks on `bus=1 / 0x3C2 mux1`.
  - Previous implementation decoded `0x3C2` scroll only in CAN B handler.
  - Therefore the scroll intent often never entered the scroll-gear state machine.
- Fix implemented:
  - Added shared `handleVcleftSwitchFrame(...)`.
  - CAN A `handleRearFogCanADecelFrame(...)` now reads `0x3C2 mux1` scroll ticks.
  - CAN B still handles `0x3C2 mux0` hazard caching and can also parse mux1 if present.
  - CAN B 0x3C2 path now calls the shared parser and returns before the old duplicate logic.
- Build verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Next step:
  - Flash COM23 again before retesting.
  - First test with injection OFF and verify WebUI fields: `rightScrollTicks`, `scrollGearIntent`, `currentGear`, `brakeActive`, `vehicleSpeedKph`, `rightStalkStatus`.
- Upload:
  - Flashed COM23 with `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`.
  - Result: SUCCESS, flash verified, hard reset complete.

## 14. Capture analysis 2026-06-04: can_recording 71 scroll gear failure

- File: `can_recording (71).csv`.
- Test description: brake held, scroll down then up, then manually shift R and D.
- Capture summary:
  - 32968 rows, 12536 ms.
  - `dir=T` count 116, so firmware did transmit frames.
  - `0x229`: 120 RX + 14 TX on bus2.
  - Gear feedback `0x118`: P(1) -> R(2) at ~155270 ms -> D(4) at ~158450 ms; these gear changes align with manual stalk use, not the scroll injection.
- Scroll injection evidence:
  - Scroll down at ~149350 ms triggered TX `0x229` R request.
  - Scroll up at ~152650 ms triggered TX `0x229` D request.
  - Therefore scroll recognition, injection switch, and safety gates were passing.
- Failure cause:
  - Injected `0x229` counter was not monotonic:
    - R TX started with duplicate counter 9 on status 1/2/2.
    - D TX started with duplicate counter 10 on status 3/4.
  - Real `0x229 SCCM_rightStalk` frames increment counter every frame.
  - CRC formula is correct, but duplicate/stale counters cause the car to reject the injected request.
- Fix implemented:
  - Added `rightStalkTxCounter`.
  - RX `0x229` syncs `rightStalkTxCounter` to live counter.
  - TX `0x229` now increments `rightStalkTxCounter` for every sent frame instead of rereading the last live RX frame each time.
- Build verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Next retest:
  - Flash COM23 again.
  - Temporarily turn OFF `reverseStrobeEnabled` to avoid extra hazard/fog TX during scroll-gear testing.
  - Test injection and confirm TX `0x229` counters are strictly incrementing.
- Upload:
  - Flashed COM23 with the counter fix using `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`.
  - Result: SUCCESS, flash verified, hard reset complete.

## 15. Capture analysis 2026-06-04: can_recording 72

- Test condition: `reverseStrobeEnabled` closed; scroll gear injection tested.
- Capture summary:
  - 24288 rows, 9021 ms.
  - `dir=T` count 61.
  - TX IDs: CAN A `0x3FD` FSD frames, CAN B `0x229` only for scroll gear; no `0x273` or `0x3C2` TX, so reverse strobe/fog interference was removed.
- Scroll events:
  - Up/forward scroll at ~160216 ms: `0x3C2 mux1 tick=+1/+7`.
  - Down/back scroll at ~162016 ms: `0x3C2 mux1 tick=-1/-7`.
- Injection evidence:
  - Forward scroll triggered D TX sequence on bus2 `0x229`:
    - status 3 cnt2, status 4 cnt3, status 4 cnt4, status 4 cnt5, then idle.
  - CRC for all TX frames is correct.
  - Status segment counters are now monotonic.
- Failure observations:
  - Gear stayed P until manual R at ~164373 ms, then manual D at ~166414 ms.
  - Original vehicle `0x229` idle frames continued during our injected D sequence:
    - e.g. TX status3 cnt2 at 160216, then RX idle cnt2 at 160254.
    - TX status4 cnt3 at 160287, then RX idle cnt3 at 160354.
    - TX status4 cnt4/cnt5 also interleaved with original idle cnt4/cnt5.
  - This likely causes the receiving module to see duplicate/conflicting counters/status and reject the injected gear request.
  - The later down/back scroll did not send R because the first D attempt cooldown was still active until about 162137 ms; the negative scroll edge happened at 162016/162116 ms, then returned to zero.
- Current hypothesis:
  - Node-style additive injection of `0x229` may not be enough because the original SCCM/right-stalk sender keeps broadcasting idle frames with the same counters.
  - Manual stalk works because the source frame itself changes status; there are no conflicting idle frames.
- Candidate next experiments:
  - Reduce or change cooldown/latch behavior so blocked scrolls can fire after cooldown.
  - Change injection pattern to full-detent only: R=status2, D=status4, matching the successful manual captures more closely.
  - Increase injection cadence briefly (e.g. 20-30 ms) to send multiple valid status frames between original idle periods.
  - If still rejected, true reliable implementation likely requires gateway/MITM suppression or replacement of original `0x229` idle frames, not just adding frames on the same bus.

## 16. cap72 + cap73 深度复核 2026-06-04 (Claude)

- **cap72 根因锁定 = 滚动 counter 完整性，不是 idle 夹断**：原车 0x229 为 10Hz、counter 每帧 +1；70ms 叠加注入使接收端看到的 counter 序列出现重复(2,2/3,3)+回退(4,5,4)，SBW 整体拒收。gear 全程没动 → 是“counter 校验后拒收”，非“取最新”。推论：提速/full-detent/缩 cooldown 都救不了根因，提速可能更糟并触发 SBW 故障。
- **cap73(自动泊车 R→D→R) 找替代命令帧 = 失败（否定结论）**：
  - 自动泊车全程 `0x229` idle，自动换挡不走拨杆。
  - 用真实 3 次换挡点(R@61803 / D@83882 / R@93441) 对 CH(bus1)+BD(bus2) 全部 ID 做 整字节/半字节/任意 3-bit 子字段 扫描：唯一随挡位阶梯变化的只有 `0x118` 反馈本身。
  - 所有“匹配”候选(0x234 b2、0x213 b0、0x132、0x129 等)经变化次数核验全是计数器/振荡字段(几十~上千次跳变)，采样巧合，**没有阶梯型挡位命令字段**。
  - 结论：自动泊车挡位命令**不在 CH/BD**，最可能在 **PARTY CAN(pin 2/3)** 或 APE↔DI 私有/内部。
- **路线收敛**：(1) 优先接 PARTY CAN(pin2/3) 再抓自动泊车确认命令是否在那；(2) 否则只剩 0x229 MITM(断线在线改写,安全攸关)；(3) 在可靠换挡做成前，滚轮换挡保持“仅意图显示, inject 默认关”。
- 待修小 bug(不解决根因)：cooldown 1500→300-500ms、cooldown 内不要 latch 掉滚轮 edge。

## 17. 当前分支与未提交改动清单 2026-06-04

- **当前分支：`codex/can-a-full-recorder`**（不是 T-2CAN-WEB）。已含提交：`280e597` 架构文档、`35e9c45` CAN A 全量录制固件、`d06e28f` 倒车双闪 toggle、`c16aa46`/`7ebe69e`(从 T-2CAN-WEB 来)。
- **未提交改动（git status：`.ino` / `web_ui_page.h` / `README.md`；`工作总结.md` 在本分支未跟踪）**，包含三块叠加：
  1. **滚轮挂挡注入实验（codex）**：`0x229 SCCM_rightStalk` 两段式 R(1→2→2→2→idle)/D(3→4→4→4→idle)，`byte0=base[cnt]^offset[status]`、`byte1=(status<<4)|cnt`，TX counter 每帧自增并与 RX 同步；`0x3C2 mux1` 右滚轮在 CAN A/B 公共解析；WebUI 开关 `scrollGearSimEnabled`(默认开,仅显示)、`scrollGearInjectEnabled`(默认关)。安全门控=刹车+速度≤2km/h+目标挡不同+冷却。**cap72 实测被车拒收（counter 完整性）**。
  2. **倒车双闪 toggle 状态机**：`REVERSE_HAZARD_CLICK_MS`=200ms、`REVERSE_HAZARD_ON_MS`=2500ms；按一下开→保持→按一下关（替代旧的 4 次脉冲，避免开/关/开/关只看到 2 段亮）。
  3. **CAN1 只收不发开关（本次新增, Claude）**：config `can1ReceiveOnly`(默认关)、Prefs key `can1RxOnly`、`twai_send()` 入口 `if(g_config.can1ReceiveOnly) return false;`、WebUI 复选框「CAN1(13/14) 只收不发」+ /status 回传。打开=屏蔽 CAN A 全部 TX(FSD/偏移/预热)，只留 RX/录制。用于接 PARTY 等陌生总线被动嗅探。
- 编译：`pio run -e lilygo_t2can_arduino_webui` SUCCESS，Flash 23.9%；已烧 COM23（普通 WebUI 固件，注入默认关）。
- **是否提交：用户尚未决定**（这批未提交改动横跨 codex + Claude）。

## 18. 接线 / 抓 PARTY 计划（下一步）

- 只有两路 CAN：CAN A=TWAI(默认 13/14 CH)、CAN B=MCP2515(默认 9/10 BD)。
- 抓 PARTY(pin 2/3) 找自动泊车挡位命令：**把 CAN A 改接 13/14→2/3(PARTY)，CAN B 留 9/10(BD)**（BD 仍有 0x118 挡位反馈用于时间对齐）。
- 步骤：① 接线前断电；② WebUI 勾「CAN1 只收不发」(或烧 `lilygo_t2can_cana_full_recorder`)；③ 抓包 ID 留空=全量；④ 做自动泊车 R→D→R 下载 CSV；⑤ ⚠️ 确认 PARTY 也是 500kbps（看 CAN1 RX 计数是否在涨，不涨=速率/接线不对）。
- 若 PARTY 也无挡位命令 → 命令在 APE↔DI 私有/内部，只能 0x229 MITM 或滚轮换挡停在“仅意图显示”。

## 19. cap74 PARTY(2/3) 抓包结论 2026-06-04 — 三条总线全部排除

- 接线：CAN A=PARTY(2/3)=bus1，CAN B=BD(9/10)=bus2。自动泊车 R@96652 / D@114810 / R@123989。
- **PARTY(2/3) 只有 6 个 ID**：273/280/325/328/389/599，基本是与 CH 重叠的底盘子集；唯一新 ID `0x148` 是计数器帧(b0/b1 自增,b2-7 不变)。帧量少(12992)。
- 3 点扫描 PARTY：候选(0x111 b1=255/0、0x145、0x185、0x257 等)经核验全是计数器/噪声字段的采样巧合，**无阶梯型挡位命令**；只有 0x118 反馈随挡位变化。
- **三条可接触总线全部排除挡位命令**：CH(13/14)❌、BD(9/10)❌、PARTY(2/3)❌。
- **最终判定**：自动泊车挡位命令在接不到的总线(APE↔DI 私有/DI 内部)，当前硬件**无法注入**。真换挡只剩 **0x229 MITM**(BD 断线在线改写,保留 counter,安全攸关)。
- **决策建议**：滚轮换挡定型为“仅意图显示”(inject 默认关)；真换挡若做则独立上 MITM 工程。

## 20. Commit record 2026-06-04

- Branch: `codex/can-a-full-recorder`.
- Commit: `953e885 feat: document dual-can recorder and scroll gear diagnostics`.
- Committed files:
  - `ESP32S3CAN-FSD/ESP32S3CAN-FSD.ino`
  - `ESP32S3CAN-FSD/web_ui_page.h`
  - `README.md`
- Contents:
  - A/B dual full recorder behavior and README documentation.
  - Scroll gear dry-run diagnostics and experimental `0x229` injection controls.
  - CAN1 receive-only switch.
  - WebUI fields for scroll ticks, right-stalk status/counter, gear, brake, speed, and injection status.
- Verification before commit:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Local note:
  - `工作总结.md` updated locally only and not included in the commit.

## 21. Upload record 2026-06-04

- Branch: `codex/can-a-full-recorder`.
- Firmware: `lilygo_t2can_arduino_webui`.
- Command: `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`.
- Result: SUCCESS, ESP32-S3 detected, flash verified, hard reset complete.

## 22. Capture analysis 2026-06-04: can_recording 75

- Wiring/test:
  - CAN A = CH 13/14, CAN B = BD 9/10.
  - Vehicle started in D, recording started, Autopark engaged, Autopark shifted R, then finally D, then P near the end.
- Capture summary:
  - 87102 rows, 32523 ms.
  - bus1/CH: 10067 rows, 8 RX IDs.
  - bus2/BD: 77035 rows, 314 RX IDs.
  - No TX frames in this capture.
- Gear feedback timeline from `0x118`:
  - D at 143989 ms.
  - R at 146339 ms.
  - D at 168408 ms.
  - P at 175207 ms.
- `0x229 SCCM_rightStalk`:
  - Non-idle count = 0.
  - Around Autopark R/D transitions, `0x229` remains status=0 idle with rolling counter.
  - Confirms Autopark does not use the manual right-stalk `0x229` path.
- Field scan:
  - Stable D/R/D byte candidates: `0x118` gear feedback and `bus2 0x108` byte6/byte7.
  - Bitfield scan also found `0x1E5`, `0x3E3`, `0x399`.
  - Timing review:
    - `0x1E5` contains gear-like feedback (`D=4/R=2/P=1`) but changes ~8-11 ms after `0x118`; it is feedback/status, not command.
    - `0x108`, `0x3E3`, and `0x399` change after or with gear feedback and behave like state/motion feedback, not command source.
- Conclusion:
  - cap75 reinforces the previous result: CH(13/14) + BD(9/10) do not carry the Autopark gear command.
  - Available frames expose gear feedback/status only; the actual Autopark command remains on an inaccessible/private path or inside DI/APE logic.

## 20. BD_ON_CAN1 实验分支 2026-06-04 (Claude)

- **假设**：别人固件能换挡，可能因为把 BD(9/10) 接到 **CAN1/TWAI**(时序好、RX队列大、无 SPI 中间层)，而我们默认把 BD 放在 CAN2/MCP2515(小 buffer、SPI、每轮 drain 预算小)，对 0x229 这种 counter/时序敏感的注入不利。
- **新分支 `BD_ON_CAN1`**（从 `codex/can-a-full-recorder` 切出）+ **新环境 `lilygo_t2can_bd_on_can1_webui`**（extends webui，加 `-DBD_ON_CAN1`）。
- **最小改动（全部 `#ifdef BD_ON_CAN1`）**：
  - `canb_send()` → `twai_send()`；`canb_recv()` → `twai_recv()`（灯光/拨杆/滚轮挂挡逻辑一行不动，整体改走 TWAI）。
  - `twai_recv()` 在 BD_ON_CAN1 下不做 isRelevantCanId 软过滤；setup 用 `TWAI_FILTER_CONFIG_ACCEPT_ALL()`。
  - setup 下 `canbReady=true` 且**不** init MCP2515（CH 在 MCP2515 上,本实验不用）。
  - `drainCanBWithBudget()` 预算 4→48，且每帧同时喂 `handleRearFogCanADecelFrame`(更新 gear/brake/speed 供安全门控) + `handleCanBFrame`(拨杆/滚轮/0x3C2)。
  - `handleRearFogCanADecelFrame` 的 0x3C2 块在 BD_ON_CAN1 下用 `#ifndef` 跳过(避免与 handleCanBFrame 双处理滚轮 edge)。
  - loop 在 BD_ON_CAN1 下只跑灯光/拨杆/滚轮挡 stack，**不发** FSD/速度偏移/电池预热。
- 编译：`lilygo_t2can_bd_on_can1_webui` SUCCESS(Flash 23.8%)；普通 webui 回归 SUCCESS。已烧 COM23。
- **接线（必须断电改）**：BD `9/10` → 板 CAN1/TWAI(GPIO7/6)；CH `13/14` → 板 CAN2/MCP2515(本实验不用)。
- **测试步骤**：WebUI 确认 `can1ReceiveOnly` **关**(否则 twai_send 被拦)、`scrollGearInjectEnabled` **开**；踩刹车、单次上滚或下滚；看 WebUI `rightScrollTicks/rightStalkStatus/currentGear/brakeActive/vehicleSpeedKph/scrollGearInjectActive` 是否正常，以及车是否真的换挡。
- **注意**：仍非 MITM，原车 0x229 idle 仍在；只是 TWAI 时序更好，验证是否更接近别人固件成功条件。若仍被拒 → 基本确认必须 MITM。
- 提交状态：分支已建、改动**已落盘**(本会话编辑)；是否 commit 由用户测后决定。

## 21. cap76/77/78 实测（BD 已接 TWAI，bus1=BD）— BD_ON_CAN1 注入仍失败，定论

- 接线已生效：三份抓包所有帧都在 bus1(=CAN1/TWAI=BD)，bus2(MCP2515/CH) 为空。
- **cap78 手动 D→R（黄金参考）**：0x229 由唯一 SCCM 自己改 status，counter 一路连续：
  - D：`s=3 c=5 → s=4 c=6,7,8,9`；R：`s=1 c=14,15 → s=2 c=0,1,2 → s=1 c=3,4`。我们的注入 status pattern(R:1→2→2→2 / D:3→4→4→4) 与之基本一致。
- **cap77 自动泊车 D→R→D**：0x229 **全程 idle**(status 全 0) → 自动泊车不走 0x229(APE/DI 私有)，无法复制。
- **cap76 滚轮注入(14 帧 TX)**：挡位**全程停 P，未换挡**；TX 与原车 idle RX 在**同一 counter 上各出现一次**(c=0 我们s3 + c=0 原车s0 …) → 与 MCP2515 时完全相同的 counter 冲突。**证明 TWAI 时序不是关键，BD_ON_CAN1 对换挡注入无帮助。**
- **根因终判**：你这台车**有物理拨杆**，SCCM **持续 10Hz 发 idle 0x229**；任何叠加注入都会与之 counter 冲突被 SBW 拒收。叠加注入已被 **MCP2515 + TWAI 两种方式双双证伪**。
- **“别人固件能换挡”最可能解释**：那台车**无物理拨杆**(如 Highland)，车上没有 SCCM 持续发 0x229，ESP32 是唯一发送方 → 叠加=唯一来源 → 成功。有拨杆的车不适用。
- **结论/路线**：有拨杆车上真换挡**只能 0x229 MITM**(断线在线改写 status、保留 SCCM counter、抑制原帧)；否则滚轮换挡定型为“仅意图显示”。
- 分支 `BD_ON_CAN1` 仅作实验，结论为“此路不通(对有拨杆车)”。

## 2026-06-06 Battery preheat capture 91/92 analysis

- Branch: `T-2CAN-WEB`.
- Capture files:
  - `can_recording (91).csv`: not battery preheat. It only contains `0x339` service/repair-mode traffic on `bus=2/MCP2515/physical CANA`.
  - `can_recording (92).csv`: battery preheat `0x082 UI_tripPlanning` on `bus=2/MCP2515/physical CANA`, RX only.
- 92 decoded sequence:
  - OFF/context: `01 50 91 33 FF 03 EE 04`.
  - Transitional/incomplete request: `AE 50 91 80 FF 03 00 80` (`tripPlanningActive=0`, ambient/energy SNA).
  - Valid ON: `AF 50 91 33 FF 03 EE 04`.
- Important conclusion:
  - `byte0` carries the request bits (`0xAF` ON, `0x01` OFF).
  - `byte1..7` are route/context fields and changed across captures (`94 31 B4 05`, `91 33 EE 04`, etc.).
  - Prior firmware sent a fixed 1000 ms payload; earlier WebUI captures also showed the vehicle continuing to broadcast inactive `0x082`, so the injected ON can be overwritten.
- Code change:
  - Firmware now caches the latest usable vehicle `0x082` context (`byte1..7`) from CAN B RX.
  - Battery preheat TX reuses the cached context and only overrides `byte0`.
  - TX period changed from 1000 ms to 200 ms while enabled.
  - Fallback context set to latest valid 92 OFF frame: `01 50 91 33 FF 03 EE 04`.
- Build verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.

## 40. PR #8 README merge conflict resolution 2026-06-06

- Branch: `T-2CAN-WEB`.
- PR: `T-2CAN-WEB -> main` in `hqs17017301634-max/Arduino-waveshare_ESP32_S3_RS485_CAN-Arduino`.
- Conflict scope:
  - `README.md` only.
  - Conflict was between `main` repository architecture documentation and `T-2CAN-WEB` branch-specific project architecture documentation.
- Resolution:
  - Kept `main` `Repository Architecture` / `仓库架构` sections to describe the baseline branch family.
  - Kept `T-2CAN-WEB` `Project Architecture` / `项目架构` sections to describe the active dual-CAN WebUI branch.
  - Preserved the corrected physical bus naming:
    - `bus=1 / TWAI / physical CANB`
    - `bus=2 / MCP2515 / physical CANA`
- Verification:
  - Conflict markers removed from `README.md`.
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Git note:
  - `工作总结.md` is updated as a local record and should not be staged for the PR merge commit unless explicitly requested.

## 40. Battery diagnostic display polish 2026-06-06

- Branch: `T-2CAN-WEB`.
- Fixed `0x082 UI_battPreconditionOnNavPowerReq` display:
  - Raw `0x7F` now maps to sentinel `-32768`, so WebUI displays `SNA` instead of `15875 W`.
  - Target temperature / destination ambient `-32768` also displays as `SNA`.
- WebUI localization/polish:
  - New battery diagnostic labels changed from English to Chinese.
  - `BMS temp ID` now displays in hex, e.g. `0x712` instead of decimal `1810`.
  - `batteryPreheatUiState` and fast-charger type display Chinese enum labels.
  - Added derived `0x712` raw group display: `mux N / b1-b7 ...`.
- Build verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.

## 22. 0x229 注入 counter 修复 + 调参 2026-06-04 (Claude) — 关键转折

- **用户实测：这台有挡 HW3 车换挡是可实现的**（之前“叠加注入不可能”的结论作废；SBW 接收端容忍原车 idle 并存，但不容忍我们注入流自身 counter 乱跳）。
- **用 cap78 手动 D→R 验证我们注入参数**：CRC 表(base/offset)100% 正确；status 模式正确(D=3→4444 / R=1→222→11)。
- **定位 bug（cap76 证据）**：handleCanBFrame 收到每帧原车 0x229 idle 都执行 `rightStalkTxCounter=counter`，注入期间把我们的 TX counter 往回拽 → 我们自己发出的帧 counter 重复(c=2 两次) → 被接收端拒。手动是单发送方 counter 连续故能换挡。
- **修复**：`if (!scrollGearShiftActive) rightStalkTxCounter = counter;`（只在非注入时对齐；burst 内严格 +1 自增）。
- **调参**：`SCROLL_GEAR_STATUS_FRAMES 4→6`、`SCROLL_GEAR_FRAME_INTERVAL_MS 70→50`（~20Hz 压过原车 10Hz，detent 维持更久贴近手动）。
- 环境 `lilygo_t2can_bd_on_can1_webui`(BD 接 TWAI) 编译 SUCCESS、已烧 COM23。
- **待用户实车测**：驻车踩刹车→单次下滚试 R；看 `当前挡位 0x118` 变 R、`实验注入中=1`、counter 单调。抓 553,280 回传核对。
- 换挡 ID 速查：**0x229(553)=SCCM_rightStalk 命令**（byte1 hi=status R1/2 D3/4，lo=counter，byte0=CRC）；**0x118(280)=挡位反馈**(byte2 bit5-7：2=R 4=D)。

## 23. cap80/81 对比 → DLC 修复 2026-06-04 (Claude)

- cap81(我们注入,counter修复后)：TX counter 已单调(1..9)✅，但挡位仍未变(停 D)。
- cap80(手动成功)：R=`s1,s1,s2,s2,s2` counter 3-7 连续、burst 内无 idle(单发送方)；D=`s3,s3,s4,s4,s4,s4`。
- **关键差异 = DLC**：真车 0x229 **DLC=3**，我们注入是 **DLC=8** → 接收端极可能因 DLC 不符丢帧。
- **修复**：`rightStalkFrame` `can_dlc 8→3`。现在单帧格式(CRC+status+counter+DLC)与手动完全一致。
- 仍存在的隐患：原车 idle 逐帧夹入使总线 counter 出现重复(我cN+原cN)。若 DLC 修复后仍不换 → 接收端严格校验 counter 连续性 → 只能 MITM 抑制原帧。
- 环境 `lilygo_t2can_bd_on_can1_webui` 已烧 COM23，待实车测：踩刹车单次下滚试 R，看 0x118 是否变 R，抓 553,280 回传。

## 24. 滚轮换挡移植到 T-2CAN-WEB (方案B 验证) 2026-06-04

- 关键修复 = **DLC=3**（cap82 实测 P→R→D 成功；counter 单调是前提，CRC/status 早对）。
- T-2CAN-WEB 原本**完全没有**滚轮换挡代码（serviceScrollGearShift/rightStalkFrame/0x229 全 0）。
- 用户决定：滚轮换挡走 **BD→MCP2515(标准,保留FSD)**；先验证(B)再清源码(A)。
- 方案B 做法：`git checkout ce25a2a -- .ino web_ui_page.h`（BD_ON_CAN1 已验证文件，是 T-2CAN-WEB .ino 的严格超集）；**保留 T-2CAN-WEB 的 platformio.ini**(PSRAM + REC_CAP=100000，无 recorder/BD_ON_CAN1 env)。录制器/BD_ON_CAN1 的 #ifdef 代码进源码但标准 env 不编译。
- BD_ON_CAN1 已提交 `ce25a2a`(实验分支保存)。
- 编译 `lilygo_t2can_arduino_webui` SUCCESS(Flash 23.9%)，已烧 COM23。**T-2CAN-WEB 改动未提交**(先验证)。
- ⚠️ **测试前必须把线改回标准**：BD 9/10→CAN2/MCP2515，CH 13/14→CAN1/TWAI。滚轮换挡这版走 MCP2515(注入未实测，DLC 修复总线无关应能成)。
- 待办：B 验证通过后执行 **A**——删除 .ino 里 recorder/BD_ON_CAN1 的 inert #ifdef 代码，源码纯净化。

## 25. 滚轮换挡 方案A 源码纯净化 2026-06-04 (T-2CAN-WEB)

- 方案B 实车验证 MCP2515 路也能换挡(用户确认“可以用”)。
- 方案A：删除 .ino 里 `#ifdef BD_ON_CAN1` 与 `ENABLE_CANA_FULL_RECORDER` 全部实验代码，只留正常 MCP2515 路径。
  - twai_recv 软过滤恢复无条件；canb_recv/canb_send 只留 MCP2515；drainCanBWithBudget/setup/loop 去实验分支；handleRearFogCanADecelFrame 的 0x3C2 恢复无条件；applyBuildModeGuards 变空操作。
  - 保留：滚轮换挡(MCP2515,DLC=3)、基础 CSV 录制、can1ReceiveOnly、灯光全功能。platformio.ini 未动(PSRAM+REC_CAP=100000)。
- 编译：webui / lilygo_t2can_arduino / waveshare 全 SUCCESS(Flash 23.9%,删的是 inert 死代码)。已烧 COM23。
- 滚轮换挡最终配置(T-2CAN-WEB 标准)：CH 13/14→TWAI(FSD)，BD 9/10→MCP2515(灯光+0x229 换挡)；DLC=3 + counter 单调是关键。
- 未提交：.ino / web_ui_page.h / 工作总结.md（待用户确认提交）。

## 26. HW3 speed offset / slew analysis 2026-06-05

- Current branch checked: `T-2CAN-WEB`; local dirty files remain `ESP32S3CAN-FSD/ESP32S3CAN-FSD.ino` and `ESP32S3CAN-FSD/web_ui_page.h`.
- Compared `ESP32-FSD` branch with current `T-2CAN-WEB` source by `git show`, without switching branches.
- HW3 speed compensation core is the same path on both branches:
  - CAN A/TWAI receives `0x399` fused speed limit, `1016` follow distance, and `1021` AP control mux frames.
  - `0x399 data[1] & 0x1F` is valid when not `0` or `31`; effective limit is `raw * 5 kph`.
  - `1021 mux0` sets FSD bit 46 and writes speed profile into `data[6] bit1..2`.
  - `1021 mux1` clears bit 19.
  - `1021 mux2` writes speed offset raw into `data[0] bit6..7` + `data[1] bit0..5`.
- Target table/defaults:
  - `<60 -> 60`, `60..69 -> 80`, `70..79 -> 85`, `80..89 -> 90`, `90..99 -> 100`, `100..119 -> 120`, `120..139 -> 140`, `>=140 -> same as limit`.
  - Desired offset is clamped to `0..25 kph`.
  - For limit `<50 kph`, raw is forced to `200` (50% PCT4), so reported `offsetKph` and CAN raw equivalent can differ.
  - For limit `>=50 kph`, raw = `round(offsetKph / limit * 100) * 4`.
- Downward slew:
  - ESP32-FSD uses fixed `5%/s`; T-2CAN-WEB exposes `slewPctPerSec`, default still `5`.
  - Only target raw decreases are rate-limited; increases apply immediately.
  - Raw step rate is `slewPctPerSec * 4 raw/s`; default is `20 raw/s`.
- T-2CAN-WEB differences:
  - WebUI/NVS makes FSD enable, auto offset enable, target table, low-speed raw, and slew rate tunable.
  - `can1ReceiveOnly=true` blocks all CAN A/TWAI TX, therefore disables FSD activation, speed offset, and battery preheat sending.
  - CAN B/BD features do not participate in HW3 speed offset logic.

## 27. Autopark/Summon vs FSD model and capture value 2026-06-05

- Inference, not official Tesla architecture: FSD driving, Autopark, and Summon likely share some perception/occupancy inputs, but their vehicle-state machines and actuation authority are different.
- Existing captures show Autopark can shift R/D while `0x229` right-stalk remains idle; therefore Autopark/Summon gear authority is not the same public SCCM/right-stalk path as manual shifting.
- CH(13/14), BD(9/10), and PARTY(2/3) captures did not expose a reusable Autopark gear command; visible gear-related frames are feedback/status such as `0x118`, not command.
- Summon capture is still useful, but the goal should change:
  - Not expecting a directly reusable R/D command on current exposed buses.
  - Useful to identify Summon/Autopark active state, mode transitions, brake/hold/ready states, and timing before gear feedback changes.
  - If a private command is not exposed, these states can only serve as gating/context for our own logic, not as the actual shift command.
- FSD-state caveat: if physical stalk shifting is blocked while FSD/AP is active, then `0x229` emulation may also be blocked in that state. Testing should separate normal low-speed shift, AP/FSD engaged state, and Summon/Autopark state.

## 28. Battery preheat unavailable check 2026-06-05

- Previous battery-preheat capture exists locally: `C:\Users\Administrator\Downloads\can_recording (20).csv`.
- `can_recording (20).csv` contains explicit `0x082` frames on `bus=2`:
  - ON:  `AF 50 AC 3C FF 03 9A 0F` at ~1000 ms period.
  - OFF: `01 50 AC 3C FF 03 9A 0F` after stopping.
- `can_recording (64).csv` also contains `0x082` on `bus=2`, but payload is baseline/inactive-looking `00 50 AC 80 FF 03 00 80`, not the ON/OFF pair above.
- Current firmware has the captured ON/OFF payloads but `sendBatteryPreheatFrame()` uses `twai_send()`, therefore sends on CAN A/TWAI.
- Likely issue if WebUI battery preheat is unavailable: the real `0x082 UI_tripPlanning` observed for preheat is on `bus=2`/CAN B in the capture, while current code transmits it on CAN A.
- Also check WebUI `can1ReceiveOnly`: when true it blocks CAN A/TWAI TX, but if preheat is moved to CAN B this switch should not block it.
- Fix applied 2026-06-05:
  - `sendBatteryPreheatFrame()` now sends via `canb_send()` when `ENABLE_CANB_MCP2515` is enabled; non-CAN-B builds fall back to `twai_send()`.
  - WebUI hint changed from CAN A to CAN B for battery preheat.
  - Build `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - Upload `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`: SUCCESS, flash verified, hard reset complete.

## 29. LILYGO T-2CAN physical CANA/CANB naming correction 2026-06-05

- User-provided board image shows physical screw terminals labeled `CANA` and `CANB`.
- Cross-check against official LILYGO T-2Can GitHub:
  - `pin_config.h`: native CAN/TWAI pins are `CAN_TX=7`, `CAN_RX=6`; SPI is `SCLK=12`, `MOSI=11`, `MISO=13`, MCP2515 `CS=10`, `RST=9`, `INT=8`.
  - Official `examples/can/can.ino` names MCP2515 object `Can_A(...)` and TWAI init/send/receive functions `Can_B_*`.
- Therefore official/physical naming is:
  - Physical `CANA` = MCP2515/SPI channel.
  - Physical `CANB` = ESP32-S3 native TWAI channel.
- Our firmware/documentation previously used an internal naming scheme:
  - Software `CAN A` / `bus=1` = TWAI.
  - Software `CAN B` / `bus=2` = MCP2515.
- This is reversed relative to LILYGO physical silk naming and must be corrected in future code comments/WebUI/README to avoid wiring mistakes.
- Immediate implication:
  - The 2026-06-05 change `0x082 -> canb_send()` sends through our software `CAN B` = MCP2515 = physical `CANA`, not physical `CANB`.
  - If the intended target is physical `CANB`, `0x082` must use `twai_send()` instead.

## 30. Official LILYGO T-2Can V1.0 hardware review 2026-06-05

- Official repo checked: `Xinyuan-LilyGO/T-2Can`, hardware version `T-2Can_V1.0`.
- Official README says T-2Can is ESP32-S3 with two CAN buses; T-2Can V1.0 CAN module is MCP2515 over SPI; T-2Can-FD uses MCP2518.
- Official example naming:
  - `Can_A` = MCP2515/SPI.
  - `Can_B_*` = ESP32-S3 TWAI.
- Correct physical mapping for T-2Can_V1.0:
  - Physical `CANA` terminal = MCP2515/SPI, pins SCLK12/MOSI11/MISO13/CS10/RST9/INT8.
  - Physical `CANB` terminal = native TWAI, pins TX7/RX6.
- Current project optimization recommendations:
  - Rename WebUI/README/status from ambiguous `CAN A/CAN B` to physical `CANA(MCP2515)` and `CANB(TWAI)`, or expose both physical and software labels.
  - Keep high-load/time-critical FSD and speed offset on native TWAI/physical CANB.
  - Keep body/lighting/right-stalk/preheat on MCP2515/physical CANA if captures confirm those frames are on that physical bus.
  - Add MCP2515 INT GPIO8 support instead of polling-only drain; keep software/hardware filters for high-load body bus.
  - Add `0x082` to MCP2515 feature filter if battery-preheat monitoring/recording is needed while filters are ON.
  - Check fixed 120-ohm termination on both CAN channels before tapping Tesla buses; avoid adding a third terminator to an already terminated bus.

## 31. T-2CAN hardware/software optimization backlog 2026-06-05

- Conclusion: there are still useful optimizations. The highest-priority item is not performance but eliminating CANA/CANB naming ambiguity.
- Must-fix:
  - WebUI/README/platform comments should use physical names: `CANA(MCP2515/SPI)` and `CANB(TWAI/GPIO7/6)`.
  - CSV/export/debug status should show both internal bus number and physical channel, e.g. `bus=1/TWAI/physical CANB`, `bus=2/MCP2515/physical CANA`.
  - Battery preheat route must be validated against physical wiring. Current `canb_send()` means MCP2515/physical CANA in this project.
  - Stale comments need correction: battery preheat period is 1000 ms, and `can1ReceiveOnly` no longer blocks preheat if preheat is sent by MCP2515.
- High-value software optimizations:
  - Add MCP2515 INT GPIO8 support to reduce polling and lower RX latency.
  - Increase/adapt MCP2515 RX drain budget when recorder is active or INT remains asserted.
  - Add `0x082` to MCP2515 feature filter if preheat capture/monitoring is required with filter ON.
  - Add per-bus capture filters, drop counters, buffer-full reason, TWAI bus state, and MCP2515 error/overflow status to WebUI.
  - Consider a WebUI-selectable battery-preheat route: auto / `CANA(MCP2515)` / `CANB(TWAI)`.
- Hardware checks:
  - Measure CANH-CANL resistance on each tapped vehicle bus before long testing.
  - If the board has fixed 120-ohm termination and the Tesla bus is already terminated, avoid leaving the T-2CAN channel as an extra terminator.
  - Keep isolated CAN grounds separated according to the actual harness; do not unintentionally tie `SGNDA` and `SGNDB` together.

## 32. T-2CAN physical channel naming and MCP2515 optimization applied 2026-06-05

- Branch/worktree: `T-2CAN-WEB` in `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040`.
- Channel naming correction applied in code-facing UI/docs:
  - CSV `bus=1` = ESP32-S3 native TWAI = official/physical `CANB`.
  - CSV `bus=2` = MCP2515/SPI = official/physical `CANA`.
  - WebUI now shows the mapping explicitly and CSV download adds `controller` and `physical` columns.
  - `platformio.ini` comments now use physical `CANA(MCP2515)` / `CANB(TWAI)` mapping and defines `MCP2515_INT=8`.
- MCP2515 optimizations:
  - Added MCP2515 INT GPIO8 input setup.
  - Normal MCP2515 RX drain remains 4 frames per loop.
  - When MCP2515 INT is asserted or recorder is active, drain budget increases to 24 frames with a 900 us time cap.
  - WebUI/status now exposes MCP2515 EFLG and RX overflow count.
- MCP2515 hardware filter:
  - Replaced old boolean with `canbFilterMode`: `0=all`, `1=feature IDs`, `2=minimum runtime IDs`.
  - Old `canbFilterEnabled` API/status is kept for compatibility.
  - Added `0x082 UI_tripPlanning` to feature/minimum MCP2515 filters so preheat monitoring is not filtered out.
- Recorder:
  - `/rec_status` now reports dropped frames, per-bus counts, stop reason, PSRAM bytes.
  - CSV header changed from `ts_ms,dir,bus,...` to include `controller` and `physical`.
- Battery preheat route:
  - Current T-2CAN build sends `0x082` via `canb_send()` = CSV `bus=2` = MCP2515 = physical `CANA`.
  - This matches previous preheat capture where `0x082` appeared on `bus=2`.
- Verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino`: SUCCESS.
  - `pio run -e waveshare_ESP32_S3_RS485_CAN`: SUCCESS.
  - Upload `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`: SUCCESS, flash hash verified, hard reset complete.

## 33. T-2CAN-WEB deep review follow-up 2026-06-05

- Review scope: current dirty `T-2CAN-WEB` code after physical CANA/CANB rename and MCP2515 INT/filter work.
- Fixed during review:
  - `serviceBatteryPreheat()` now respects `canbEnabled` in T-2CAN builds. If MCP2515/physical CANA is disabled, `0x082` no longer continues to transmit through `canb_send()`.
  - WebUI recorder hint now clarifies that clearing ID input only disables recorder-layer filtering; `bus=1/TWAI` still records firmware-relevant IDs, while `bus=2/MCP2515` breadth depends on MCP2515 hardware filter mode.
  - README recorder notes updated with the same bus=1/bus=2 limitation.
- Verification after fix:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino`: SUCCESS.
  - `pio run -e waveshare_ESP32_S3_RS485_CAN`: SUCCESS.
  - `git diff --check`: clean except CRLF conversion warnings.
- Remaining cleanup candidates:
  - `handleCanBFrame()` contains unreachable old duplicated `0x3C2` parsing code after `handleVcleftSwitchFrame(...); return;`; runtime is unaffected, but it should be removed in a dedicated source cleanup pass.
  - Several old Chinese comments in `.ino` are mojibake/garbled; not a compile issue but hurts maintenance and makes patching fragile.
  - If true full bus=1 capture is required, current WebUI recorder is not enough because `twai_recv()` still applies firmware ID filtering before `recordCanFrame()`.

## 34. Reverse hazard strobe duplicate-trigger fix 2026-06-05

- User-reported symptoms:
  - Scroll-wheel reverse trigger produced only one hazard flash, while physical stalk reverse produced about four flashes and stopped.
  - When already in R, triggering scroll-wheel reverse again could leave hazards flashing continuously.
- Root cause found in `ESP32S3CAN-FSD.ino`:
  - Reverse hazard uses `0x3C2` `hazardButtonPressed` as a momentary toggle button, not a direct ON/OFF signal.
  - Scroll-wheel reverse can trigger `startReverseStrobe()` before the injected `0x229` shift finishes.
  - When the gear feedback later changes to R on `0x118`, the gear-edge path called `startReverseStrobe()` again while the first reverse strobe was already active.
  - The second start sent another hazard button press, toggling hazards back off early; the timed "OFF" click later could toggle hazards on again, matching the observed one-flash / non-stop behavior.
- Fix applied:
  - Added `reverseHazardLatchedOn` to track whether this feature believes it has toggled hazards on.
  - Made `startReverseStrobe()` idempotent while a reverse strobe phase is active, so scroll-trigger + R-gear-feedback cannot double-click the hazard button.
  - `stopReverseStrobe(true)` now releases any in-progress press and, if this feature latched hazards on, sends one explicit OFF click plus release before clearing rear fog.
  - Normal phase-2 timeout now clears `reverseHazardLatchedOn` when the scheduled OFF click is sent.
- Verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino`: SUCCESS.
  - `pio run -e waveshare_ESP32_S3_RS485_CAN`: SUCCESS.

## 35. Scroll R/D same-gear trigger guard 2026-06-05

- User request: scroll-wheel R/D trigger should ignore same-gear requests. If current gear is already R, scroll-back/R must not trigger; if current gear is already D, scroll-forward/D must not trigger.
- Existing code already blocked same-gear `0x229` injection inside `scrollGearSafetyOk()` with block reason `4`, but reverse hazard/rear-fog logic was triggered separately by `0x3C2` scroll ticks before that safety gate.
- Fix applied in `handleVcleftSwitchFrame()`:
  - On brake + scroll edge, compute target gear from scroll direction.
  - If `g_status.currentGear == targetGear`, do not call `requestScrollGearShift()`.
  - Same-gear requests set WebUI block reason `4` and clear the displayed gear intent.
  - Reverse hazard start now also checks same-gear: already in R + scroll-back will not start reverse hazard/rear-fog.
  - D scroll can still stop an already-active reverse strobe, so it remains a useful cancel path.
- Verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino`: SUCCESS.
  - `pio run -e waveshare_ESP32_S3_RS485_CAN`: SUCCESS.

## 36. COM23 full erase + upload 2026-06-05

- Branch/worktree: `T-2CAN-WEB` in `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040`.
- Flashed current dirty working tree, including:
  - Reverse hazard duplicate-trigger fix.
  - Scroll R/D same-gear trigger guard.
  - Physical CANA/CANB naming and MCP2515 INT/filter/recorder changes.
  - Battery preheat MCP2515 route + `canbEnabled` guard.
- Commands:
  - `pio run -e lilygo_t2can_arduino_webui -t erase --upload-port COM23`: SUCCESS, chip erase completed, hard reset.
  - `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`: SUCCESS, all written segments hash-verified, hard reset.
- Note:
  - Full erase clears persisted NVS/WebUI settings; switches and tunables return to firmware defaults after boot.

## 37. Battery preheat capture 85/86/87 analysis and payload fix 2026-06-05

- Capture labels:
  - `can_recording (85).csv`: WebUI enabled battery preheat.
  - `can_recording (86).csv`: vehicle UI/navigation enabled battery preheat.
  - `can_recording (87).csv`: vehicle UI/navigation disabled battery preheat.
- Key finding:
  - All relevant `0x082 UI_tripPlanning` frames are on CSV `bus=2 / MCP2515 / physical CANA`.
  - WebUI capture 85 proves firmware transmitted `0x082` on the correct bus, but with the old payload:
    - TX ON:  `AF 50 AC 3C FF 03 9A 0F`
    - TX OFF: `01 50 AC 3C FF 03 9A 0F`
    - Vehicle RX baseline remained `00 7F FF 80 FF 03 00 80`, so the request was not accepted.
  - Real vehicle ON capture 86:
    - RX ON:  `AF 50 94 30 FF 03 C1 05`, 8 frames, ~1000 ms period.
  - Real vehicle OFF capture 87:
    - RX OFF: `01 50 94 30 FF 03 C1 05`, 8 frames, ~1000 ms period.
- Fix applied:
  - Updated `BATTERY_PREHEAT_ON/OFF` constants in `ESP32S3CAN-FSD.ino` to the 86/87 payloads.
  - Updated README English/Chinese payload documentation.
- Verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
  - `pio run -e lilygo_t2can_arduino`: SUCCESS.
  - `pio run -e waveshare_ESP32_S3_RS485_CAN`: SUCCESS.
- Next test:
  - Upload current firmware, enable WebUI battery preheat, then capture `0x082`; expected TX should be `AF 50 94 30 FF 03 C1 05` every ~1000 ms.
  - Upload `pio run -e lilygo_t2can_arduino_webui -t upload --upload-port COM23`: SUCCESS, hash verified, hard reset complete.

## 38. Battery preheat capture 88/89/90 correction 2026-06-05

- User clarified `can_recording (88).csv`, `(89).csv`, `(90).csv` are all battery-heating captures.
- Capture 88 was WebUI battery preheat after the previous 30/C1 firmware:
  - TX on `bus=2 / MCP2515 / physical CANA`: `AF 50 94 30 FF 03 C1 05`.
  - Vehicle RX stayed baseline: `00 50 94 80 FF 03 00 80`.
  - Conclusion: bus/route is correct, but the 30/C1 payload was still not accepted.
- Capture 89 was vehicle UI/navigation battery heat ON:
  - RX accepted payload: `AF 50 94 31 FF 03 B4 05`, about 1000 ms period.
- Capture 90 was vehicle UI/navigation battery heat OFF:
  - RX accepted payload: `01 50 94 31 FF 03 B4 05`, about 1000 ms period.
- Fix applied:
  - `BATTERY_PREHEAT_ON` changed to `AF 50 94 31 FF 03 B4 05`.
  - `BATTERY_PREHEAT_OFF` changed to `01 50 94 31 FF 03 B4 05`.
  - README English/Chinese payload documentation updated to the same 31/B4 values.
- Important note:
  - These captures do not contain useful service/repair-mode evidence (`0x339`), so service mode still needs a separate capture while toggling that feature.

## 39. Battery preheat/status diagnostics 2026-06-06

- Branch: `T-2CAN-WEB`.
- Added WebUI diagnostics for battery preheat:
  - Latest vehicle `0x082 UI_tripPlanning` seen/age.
  - Request heat bit, precondition state, nav-to-supercharger bit, fastcharger type, trip active bit.
  - Power request in W, target temperature `Cx100`, destination ambient temperature `Cx100`.
  - Template-valid flag used by dynamic `0x082` TX.
- Added passive BMS temperature-frame watcher:
  - Watches `0x312 BMS_thermalStatus`, `0x712 BMS_packTemperatureMeasurements`, and `0x374 BMS_log1` on both bus=1/TWAI and bus=2/MCP2515.
  - These IDs were also added to the CAN A/TWAI software-relevant ID list so bus=1 diagnostics are not dropped before status handling.
  - WebUI shows last seen bus, ID, mux nibble, age, and raw payload.
  - This is intentionally raw/diagnostic first because local Tesla Explorer data exposes these as ETH BMS frames and current CH/BD/CANA/CANB wiring may not receive them.
  - MCP2515 feature/minimum hardware filters do not have spare filter slots for these BMS IDs; set WebUI hardware filter mode to receive-all when testing.
- Build verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.

## 40. Battery preheat capture 93/94/95 analysis and diagnostics update 2026-06-11

- Branch: `T-2CAN-WEB`.
- Capture labels from user:
  - `can_recording (93).csv`, `(94).csv`, `(95).csv`: battery-preheat related captures.
- Key finding:
  - All `0x082 UI_tripPlanning` frames in these captures are on `bus=2 / MCP2515 / physical CANA`.
  - Manual navigation-to-supercharger frames expose usable route context:
    - active ON: `AF 50 AC 32 FF 03 61 15`
    - inactive/OFF with same context: `01 50 AC 32 FF 03 61 15`
    - transitional active with SNA destination fields: `AE 50 AC 80 FF 03 00 80`
  - Decoded values for `AF 50 AC 32 FF 03 61 15`:
    - trip active `1`
    - nav to supercharger `1`
    - fastcharger type `3` / V3
    - precondition state `1` / active heat
    - request active battery heating `1`
    - power request `10000 W`
    - target temperature `43.00 C`
    - destination ambient temperature `25.0 C`
    - charge target SNA
    - energy-at-destination raw `5473`
  - Capture 94 shows WebUI TX sending `AF 50 AC 32 FF 03 61 15` every 200 ms, but the vehicle still RX-broadcasts `01 50 AC 32 FF 03 61 15` about every 1 s. This means the injected `0x082` byte0 request shape is now correct, but `0x082` alone may not be sufficient to make the vehicle latch/accept battery preheat without the real navigation/UI backend state.
- Fixes applied:
  - Updated `BATTERY_PREHEAT_FALLBACK` and initial template to `01 50 AC 32 FF 03 61 15`.
  - Kept dynamic-template strategy: cache latest valid vehicle `0x082` context and only override byte0.
  - Improved SNA handling:
    - destination ambient `0x80` and `0xFF` are SNA.
    - energy `0x8000`, `0x8001`, and `0xFFFF` are SNA.
  - Added WebUI/status JSON diagnostics:
    - `batteryPreheatUiChargeTargetCx10`
    - `batteryPreheatUiEnergyAtDestination`
  - README English/Chinese updated with the new capture-derived payload and diagnostics.
- Verification:
  - `pio run -e lilygo_t2can_arduino_webui`: SUCCESS.
- Next test:
  - Upload to COM23, enable WebUI battery preheat, then capture `0x082`.
  - Expected WebUI TX: `AF 50 AC 32 FF 03 61 15` every 200 ms unless a newer valid vehicle template is cached.
  - Check whether vehicle feedback changes from OFF `01...` to ON `AF...`; if it remains OFF, the next target is not byte0/payload but other navigation/backend state messages that accompany real supercharger routing.

## 41. Lock-triggered deep sleep 2026-06-11

- Branch: `T-2CAN-WEB`.
- Requirement from user:
  - Board is USB-powered by vehicle.
  - When vehicle lock is detected, the firmware should stop all functions and enter ESP32 deep sleep.
  - Wake path is not CAN wake; vehicle USB power loss/return will cold-boot the board.
  - Reference only the lock-signal logic from `idf_webui_build_ascii` / `can-wifi-sleep`, not the full sleep heuristics.
- Implemented lock sources on `bus=2 / MCP2515 / physical CANA`:
  - `0x273`: `lockRequest = bits 17..19`; lock when value is `1` or `4`.
  - `0x339`: VCSEC lock state; `simpleLockStatus == 2` or `vehicleLockStatus` in `{2,5,8,10,12,15}`.
- Runtime behavior:
  - WebUI switch `lockDeepSleepEnabled` defaults OFF and is persisted in Preferences as `lockSleep`.
  - On valid lock signal while enabled: inhibit all CAN TX (`twai_send` and `canb_send` both return false), mark diagnostics, wait ~50 ms, turn WiFi off, stop/uninstall TWAI, then call `esp_deep_sleep_start()`.
  - No seat/EPAS/timeout logic was imported.
- WebUI diagnostics added:
  - enabled/armed flag
  - triggered flag
  - last lock source (`0x273` or `0x339`)
  - last lock ID
  - age since last lock signal
- MCP2515 hardware filters:
  - Feature/minimum filters now include `0x339` so VCSEC lock status is not dropped when filtering is enabled.
  - Feature filter gives priority to lock detection over `0x3F5` lighting-status feedback.
- Next validation:
  - First test with `lockDeepSleepEnabled` OFF and WebUI polling ON: lock the car and confirm source/last ID/age update.
  - Then enable and save `lockDeepSleepEnabled`: lock the car and confirm WebUI disconnects / board stops until USB power cycles.

## 42. README cleanup for T-2CAN-WEB 2026-06-11

- Branch/worktree: `T-2CAN-WEB` in `C:\Users\Administrator\Desktop\FSD-CAN\AAA-ESP32S3\RP2040`.
- User request:
  - Re-read `README.md`.
  - Delete content unrelated to, or wrong for, the current `T-2CAN-WEB` branch.
  - Use this file (`工作总结.md`) as the persistent work summary from now on.
- README cleanup performed:
  - Rewrote README title and scope to `T-2CAN-WEB`.
  - Removed old `ESP32-FSD` / single Waveshare / CAN-only framing from the branch README.
  - Removed generic branch/repository architecture sections that mixed other branch families into this branch-specific README.
  - Removed Waveshare build/upload commands and Waveshare full-16MB release references from this branch README.
  - Kept only current T-2CAN-WEB relevant material:
    - LILYGO T-2CAN target hardware.
    - Firmware bus mapping: `bus=1/TWAI/physical CANB`, `bus=2/MCP2515/physical CANA`.
    - HW3 FSD activation and speed-offset behavior.
    - CAN stability features.
    - MCP2515/physical CANA auxiliary body features.
    - Battery preheat `0x082` diagnostics and dynamic payload.
    - Lock-triggered deep sleep.
    - WebUI behavior and PSRAM recorder.
    - `lilygo_t2can_arduino` and `lilygo_t2can_arduino_webui` build/upload commands.
- Workflow correction / 工作流程修正:
  - Future sessions should read `工作总结.md` first.
    后续每次开始工作前，先读取 `工作总结.md`。
  - Future sessions should update `工作总结.md` at the end.
    后续每次工作结束后，更新 `工作总结.md`。
  - Future work summaries should be bilingual Chinese + English.
    后续工作总结采用中英双语记录。
  - The temporary `WORK_SUMMARY.md` created earlier should not be used going forward.
    之前临时创建的 `WORK_SUMMARY.md` 后续不再使用。

## 43. Work summary language rule / 工作总结语言规则 2026-06-11

- User request:
  - Use bilingual Chinese + English summaries going forward.
- Rule:
  - Every new work-summary entry should include both Chinese and English.
  - Keep entries concise but complete: branch, changed files, build/upload status, commit/push status, and remaining notes.
- 用户要求：
  - 后续工作总结采用中英双语。
- 规则：
  - 每次新增工作总结条目，都同时包含中文和英文。
  - 保持简洁但信息完整：分支、修改文件、编译/下载状态、提交/推送状态、遗留事项。

## 44. README pushed / README 已推送 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Committed README-only cleanup as `cbeec6b docs: clean T-2CAN-WEB README`.
  - Pushed `T-2CAN-WEB` to GitHub successfully.
  - `outputs/` was left untracked and was not pushed.
  - `工作总结.md` remains a local work-summary update and was not included in the README-only commit.
- 中文：
  - 分支：`T-2CAN-WEB`。
  - 已将 README 专项清理提交为 `cbeec6b docs: clean T-2CAN-WEB README`。
  - 已成功推送 `T-2CAN-WEB` 到 GitHub。
  - `outputs/` 保持未跟踪，未推送。
  - `工作总结.md` 仍是本地工作总结更新，未包含在本次 README-only 提交中。

## 45. DAS_status analysis / DAS_status 分析 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Reviewed local CAN Explorer page `model3_mcu3 / 0x399__DAS_status`.
  - Page/data confirms `0x399 DAS_status` is ETH bus, decimal ID `921`, with 26 signals, 17 enumerated signals, and 121 enum values.
  - Relevant current-firmware signal is `DAS_fusedSpeedLimit`; enum values show raw `0 = UNKNOWN_SNA` and raw `31 = NONE`.
  - Cross-checked reference HW3 code: fused/ISA speed limit is `0x399 byte1[4:0]`, scaled as `raw * 5 kph`.
  - Current firmware parsing `data[1] & 0x1F`, invalidating `0/31`, is consistent with the local Explorer data and reference HW3 implementation.
  - No firmware files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 已分析本地 CAN Explorer 页面 `model3_mcu3 / 0x399__DAS_status`。
  - 页面/数据确认 `0x399 DAS_status` 位于 ETH bus，十进制 ID 为 `921`，共有 26 个信号，其中 17 个带枚举，枚举值 121 个。
  - 当前固件最相关的字段是 `DAS_fusedSpeedLimit`；枚举显示 raw `0 = UNKNOWN_SNA`，raw `31 = NONE`。
  - 对照参考 HW3 代码确认：融合/ISA 限速来自 `0x399 byte1[4:0]`，换算为 `raw * 5 kph`。
  - 当前固件使用 `data[1] & 0x1F` 解析，并把 `0/31` 判为无效，和本地 Explorer 数据及参考 HW3 实现一致。
  - 本次未修改固件代码。

## 46. DAS_status signal list / DAS_status 信号列表 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Listed all 26 signals from local `model3_mcu3 / 0x399 DAS_status`.
  - Confirmed the signal list comes from `outputs/tesla_can_explorer_mcu3/signals.csv` and enum summaries from `enum_values.csv`.
  - No firmware files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 已从本地 `model3_mcu3 / 0x399 DAS_status` 数据中罗列全部 26 个信号。
  - 信号列表来自 `outputs/tesla_can_explorer_mcu3/signals.csv`，枚举摘要来自 `enum_values.csv`。
  - 本次未修改固件代码。

## 47. DAS_status bilingual analysis file / DAS_status 中英双语分析文件 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Created `DAS_status_0x399_analysis.md`.
  - The file documents `DAS_status / 0x399` frame purpose, all 26 signals, key enum meanings, current firmware relationship, fused-speed-limit parsing, PCT4 speed-offset behavior, and optimization notes.
  - No firmware source files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 已创建 `DAS_status_0x399_analysis.md`。
  - 文件说明了 `DAS_status / 0x399` 的报文用途、全部 26 个信号、关键枚举含义、与当前固件的关系、融合限速解析、PCT4 速度偏移逻辑和优化建议。
  - 本次未修改固件源码。

## 48. DAS_status table-format rewrite / DAS_status 表格版重排 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Rewrote `DAS_status_0x399_analysis.md` into a table-first format for easier viewing.
  - The new file uses tables for frame overview, all 26 signals, firmware-relevant signals, fused speed limit conversion, current speed target table, PCT4 encoding, key enum summaries, FSD relationship, and recommendations.
  - No firmware source files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 已将 `DAS_status_0x399_analysis.md` 重排为表格优先的版本，方便查看。
  - 新文件用表格整理了报文总览、26 个信号、与当前固件相关信号、融合限速换算、当前速度目标表、PCT4 编码、关键枚举、FSD 关系和建议。
  - 本次未修改固件源码。

## 49. DAS_status Excel workbook / DAS_status Excel 表格 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Created `DAS_status_0x399_analysis.xlsx`.
  - Workbook contains 10 sheets: frame overview, all 26 signals, firmware-relevant signals, fused limit conversion, speed targets, PCT4 encoding, all enum values, FSD relationship, recommendations, and conclusion.
  - Verified workbook structure successfully; it includes 105 enum-value rows for `0x399 DAS_status`.
  - No firmware source files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 已创建 `DAS_status_0x399_analysis.xlsx`。
  - 工作簿包含 10 个 sheet：报文总览、全部 26 个信号、固件相关信号、融合限速换算、速度目标、PCT4 编码、全部枚举值、FSD 关系、建议和结论。
  - 已验证工作簿结构正常；其中包含 `0x399 DAS_status` 的 105 条枚举值记录。
  - 本次未修改固件源码。

## 50. DAS_status docs commit / DAS_status 文档提交 2026-06-11

- English:
  - Branch: `T-2CAN-WEB`.
  - Prepared commit for `DAS_status_0x399_analysis.md`, `DAS_status_0x399_analysis.xlsx`, and `工作总结.md`.
  - `outputs/` remains local generated Explorer data and is intentionally excluded from the commit.
  - No firmware source files were changed.
- 中文:
  - 分支：`T-2CAN-WEB`。
  - 准备提交 `DAS_status_0x399_analysis.md`、`DAS_status_0x399_analysis.xlsx` 和 `工作总结.md`。
  - `outputs/` 仍作为本地 Explorer 生成数据保留，故意不纳入本次提交。
  - 本次未修改固件源码。
